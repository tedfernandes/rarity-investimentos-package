# Changelog

## [Unreleased]
- Correção dos erros de importação de React e arquivos de mídia no projeto.
- Instalação das dependências `react`, `react-dom`, `@types/react` e `@types/react-dom`.
- Adição de declarações de tipos para arquivos `.jpg`, `.png` e `.mp4` em `src/vite-env.d.ts`.
- Verificação e manutenção do alias `@` no `tsconfig.json` para facilitar os imports.

## [2026-03-27]
- Ambiente ajustado para compilar corretamente sem erros de módulos ou tipos.
